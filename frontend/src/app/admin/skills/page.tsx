import SkillClient from "@/components/admin/skills/SkillClient";

export const metadata = {
  title: "Manage Skills | Admin Panel",
};

export default function AdminSkillsPage() {
  return <SkillClient />;
}
