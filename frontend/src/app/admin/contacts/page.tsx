import ContactClient from "@/components/admin/contacts/ContactClient";

export const metadata = {
  title: "Admin Contacts | My Portfolio",
};

export default function AdminContactsPage() {
  return <ContactClient />;
}
